openapi: 3.0.0
info:
  title: Sample API
  version: 1.1.0
paths:
  /users/{id}:
    # CHANGED: 'get' removed (breaking)
    # ADDED: 'delete' (non-breaking if considered new method, but here we just removed get)
    delete:
      summary: Delete user
      responses:
        "204":
          description: Deleted
  /items:
    post:
      summary: Create item
      parameters:
        # ADDED: Required parameter (breaking)
        - name: type
          in: query
          required: true
          schema:
            type: string
      responses:
        "201":
          description: Created
